# Self-Pruning-Neural-Network-with-Learnable-Gates
Built a self-pruning neural network using learnable gates and L1 regularization to dynamically remove unimportant weights during training. Implemented in PyTorch and evaluated on CIFAR-10, demonstrating the trade-off between model accuracy and sparsity across different regularization strengths.
